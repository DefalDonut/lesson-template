name: Module tests
on: workflow_call

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Dependencies
        run: npm ci
      - name: Moduletests
        run: npm test
      - name: Test_outcome
        if: ${{success()}}
        run: echo "TEST WAS SUCCESSFUL!"
