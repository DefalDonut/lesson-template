name: Linting
on: workflow_dispatch

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Dependencies
        run: npm ci
      - name: Oxlint
        run: |
            npm run lint > lint-info.md
            cat lint-info.md
