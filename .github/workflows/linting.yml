name: Linting
on: workflow_call
permissions: 
  contents: write

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Dependencies
        run: npm ci
      - name: Oxlint
        run: |
            npm run lint
